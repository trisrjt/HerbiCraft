<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Panel - Add Tree Details</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css">
</head>
<body>

    <% if (success.length > 0) { %>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <%= success %>
            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button>
        </div>
    <% } %>

    <a href="/owners/logout" class="btn btn-danger m-3">Logout</a>


    <div class="container m-5">
        <h1 class="text-center">Admin Panel - Add Tree Details</h1>
        <form autocomplete="off" action="/trees/create" method="post" enctype="multipart/form-data" class="mt-4">
            <div class="mb-3">
                <label for="treeName" class="form-label">Tree Name</label>
                <input type="text" class="form-control" id="treeName" name="name" placeholder="Enter tree name" required>
            </div>
            <div class="mb-3">
                <label for="description" class="form-label">Small description</label>
                <input type="text" class="form-control" id="description" name="about" placeholder="Enter small description" required>
            </div>
            <div class="mb-3">
                <label for="commonNames" class="form-label">Common Names</label>
                <input type="text" class="form-control" id="commonNames" name="commonNames" placeholder="Enter common names (comma separated)" required>
            </div>
            <div class="mb-3">
                <label for="region" class="form-label">Region</label>
                <input type="text" class="form-control" id="region" name="regions" placeholder="Enter the region" required>
            </div>
            <div class="mb-3">
                <label for="uses" class="form-label">Uses</label>
                <textarea class="form-control" id="uses" name="uses" placeholder="Enter uses" rows="4" required></textarea>
            </div>
            <div class="mb-3">
                <label for="treeImage" class="form-label">Tree Image</label>
                <input type="file" class="form-control" id="treeImage" name="image" accept="image/*" required>
            </div>
            <button type="submit" class="btn btn-primary w-100">Submit Tree Details</button>
        </form>
    </div>

    <!-- Bootstrap JS (Optional) -->
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.min.js"></script>
</body>
</html>
